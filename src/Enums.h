#ifndef ENUMS_H
#define ENUMS_H

enum TOOL {PENCIL, ERASER, CIRCLE, TRIANGLE, RECTANGLE, POLYGON, MOUSE};

enum COLOR {RED, ORANGE, YELLOW, GREEN, BLUE, INDIGO, VIOLET, CUSTOM};

enum ACTION {NONE, UNDO, CLEAR, INCREASE, DECREASE, PUSHFRONT, PUSHBACK};

#endif